name: Code Quality

on:
  push:
    branches: [main, development, feature/**, bugfix/**, hotfix/**]
  pull_request:
    branches: [main, master, development, beta]
  workflow_dispatch:

jobs:
  quality:
    uses: ConductionNL/.github/.github/workflows/quality.yml@main
    with:
      app-name: openbuild
      php-version: "8.3"
      php-test-versions: '["8.3", "8.4"]'
      nextcloud-test-refs: '["stable31", "stable32", "stable33"]'
      enable-psalm: true
      enable-phpstan: true
      enable-phpmetrics: true
      enable-frontend: true
      enable-eslint: true
      enable-phpunit: true
      # Run the Hydra mechanical quality gates against this PR's diff.
      #
      # Deliberately no count here: the gate set is the composer package
      # `conduction/hydra-gates` and that package is the single source of truth
      # for which gates exist and how many actually report. A digit written into
      # prose goes stale silently and then misrepresents the coverage this repo
      # has. See https://github.com/ConductionNL/.github/tree/main/hydra-gates
      #
      # Without this, wiring the gates into `composer check:strict` would have
      # been a LOCAL-ONLY change: nothing in this repository's CI runs
      # check:strict — there is no workflow that invokes it — so every PR would
      # have gone green with the gates never having executed. A gate that only
      # runs on a developer's machine is not a gate.
      #
      # Pinned to a tag rather than `main` so a change to the gates cannot alter
      # this repo's verdict without a commit in this repo to move the pin.
      #
      # Moved v1.0.1 -> v1.2.0. This repo is the CONTROL in the v1.2.0 wave:
      # measured locally at full-tree scope, its gate-5 and gate-14 verdicts are
      # byte-identical under both pins (gate-5 PASS, gate-14 FAIL — 7). So a
      # change in this repo's Hydra Gates result after the pin move is
      # attributable to something OTHER than the gate-5 rewrite, which is
      # exactly what a control is for.
      #
      # What v1.2.0 brings that v1.0.1 lacks, beyond gate-5
      # (ConductionNL/.github#162, closing #153): the runner's COVERAGE
      # accounting — v1.0.1's summary printed only "N gate(s) failed" and could
      # not distinguish a gate that passed from one that never ran — plus a
      # countable gate-22 and gates 6/7 that stop passing on an empty scope.
      #
      # ADVANCED v1.2.0 -> v1.3.0 before merge, to land the fleet on one pin.
      # v1.2.0 gave the COVERAGE accounting but not the thing that accounting
      # asks for: `hydra-gates-require-full-coverage` (default ON) fails a run
      # when a gate whose subject matter EXISTS did not report, and its
      # contract is that a not-applicable gate must DECLARE itself. v1.0.1
      # contains ZERO `_skip` calls — no na/structural/wiring vocabulary at
      # all — so such a gate emits NOTHING and is counted as "DID NOT RUN".
      # v1.3.0 ships 36 such declarations. Measured on doriath (PR #160):
      # gates 4/24/33 went from unexplained "DID NOT RUN" to explicit NOT
      # APPLICABLE, and Hydra Gates went failure -> success on the pin alone.
      # The control argument above is unaffected: it concerns gate-5/gate-14,
      # which v1.3.0 does not touch.
      enable-hydra-gates: true
      hydra-gates-ref: v1.3.0
      # Newman stays disabled while the openbuild#33 fixes are in flight.
      # The collection bugs in this PR are fixed (uuid `@self.id` extraction,
      # `export-job` slug correction, hello-world `@self.slug` fallback). But
      # an upstream OR-side infrastructure blocker still prevents the seed
      # from running in CI:
      #
      #   1. `OpenBuild: SeedHelloWorld failed: Call to undefined function
      #      React\Async\await()` — OR's runtime-schema-API on `development`
      #      pulls `react/async` but the composer dependency isn't surfacing
      #      in the CI install, so the function isn't autoloaded.
      #   2. SQLite (`database: sqlite`) trips
      #      `[PermissionHandler] no such function: REGEXP` from OR's
      #      MagicMapper. The query uses MySQL's REGEXP operator which
      #      SQLite doesn't ship.
      #
      # File those as their own issues; once both are unblocked, flip this
      # back to `true` and re-evaluate.
      enable-newman: false
      database: sqlite
      newman-seed-command: 'php occ app:disable openbuild && php occ app:enable openbuild'
      # OpenRegister `development` carries the runtime-schema-API (auto-create
      # Register on application-type imports — OR PR #1464) and the lifecycle
      # engine (ObjectTransitionedEvent) that OpenBuild's repair steps + the
      # Newman publish step need; `main` predates both. See openbuild#29.
      additional-apps: '[{"repo":"ConductionNL/openregister","app":"openregister","ref":"development"}]'
      enable-sbom: true

      # Integration Tests (Newman) stays OFF here, deliberately, and unlike the
      # gates above this is not a default nobody chose. The `enable-newman:
      # false` further up carries two named, still-live upstream blockers
      # (OpenRegister's `React\Async\await()` not autoloading in the CI install,
      # and SQLite lacking `REGEXP` for OR's MagicMapper). Nineteen collections
      # are committed and would all meet those two failures — a guaranteed red
      # whose cause is already written down, which teaches nothing the comment
      # does not already say. Flipped back on in the commit that closes them.

      # ── Frontend Check legs ──────────────────────────────────────────────
      # `frontend-checks` defaults to `[]`, and an empty list means the shared
      # workflow emits NO "Frontend Check" job at all — so these two validators
      # ran nowhere while the run still looked complete. Both are self-contained
      # `node` scripts, which is what a leg has to be.
      # `test:l10n` was measured FAILING on this tree (source strings missing
      # from l10n/en.json, e.g. "Widgets on this page"). `check:manifest` could
      # not be measured locally — it `require`s `ajv/dist/2020`, which only
      # exists after the leg's own `npm ci`, so CI is the first place it gets a
      # real verdict.
      # `test` / `test:unit` are NOT listed: "Frontend Tests (unit)" runs them.
      frontend-checks: '["check:manifest", "test:l10n"]'

      # ── Coverage ratchet ─────────────────────────────────────────────────
      # `enable-coverage-guard` defaults to FALSE, which is why both
      # "Coverage Baseline Protection" and "Coverage Baseline Check" have only
      # ever reported `skipped`. It needs two inputs this repo did not have,
      # both added in this commit: `scripts/coverage-guard.php` (byte-identical
      # to openregister's) and `.coverage-baseline` = 57.39, this repo's own
      # measured coverage (8018 of 13971 statements) read from clover.xml in the
      # `coverage-report` artifact of run 30911188960.
      enable-coverage-guard: true
