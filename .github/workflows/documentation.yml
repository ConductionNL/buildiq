name: Documentation

on:
  push:
    branches: [documentation]
  pull_request:
    branches: [documentation]

jobs:
  deploy:
    uses: ConductionNL/.github/.github/workflows/documentation.yml@main
    with:
      cname: buildiq.conduction.nl

      docs-hosts: openbuild.conduction.nl,buildiq.conduction.nl