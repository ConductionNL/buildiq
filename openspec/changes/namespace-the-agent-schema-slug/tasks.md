# Tasks

- [x] 1.1 Move the slug in the fragment and the register's schema list
      **files**: lib/Settings/register.d/70-agent-workspace.json, lib/Settings/openbuild_register.json
- [x] 2.1 Follow the slug in the four schema constants, leaving the hermiq one alone
      **files**: lib/Controller/AgentsController.php, lib/Service/AgentChannelProvisioner.php, lib/Service/CopilotService.php, lib/Service/FlowAndAgentExportBundler.php
- [x] 3.1 Rename the row in place before the import, in both repair blocks
      **files**: lib/Repair/RenameAgentSchemaSlug.php, appinfo/info.xml
- [x] 3.2 Pin the write, the application scoping, and the two refusals
      **files**: tests/Unit/Repair/RenameAgentSchemaSlugTest.php
