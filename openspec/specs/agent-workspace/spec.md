# agent-workspace Specification

**OpenSpec changes**: [agent-workspace](../../changes/agent-workspace/)

**Status**: in-progress

## Purpose

TBD — created by change `agent-workspace`. Full requirements land in this
file when the change is archived; see
`../../changes/agent-workspace/specs/agent-workspace/spec.md` for the
current delta (`Agent`/`AgentRun` schemas, Agents page + chat panel reusing
`CopilotPanel`, transparent run log, tool-scope-never-exceeds-base-catalogue
guarantee, no-autonomous-triggers non-goal).

## Requirements

TBD — see the change's delta spec (link above) until archived.
