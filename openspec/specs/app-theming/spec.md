# app-theming Specification

**OpenSpec changes**: [app-theming](../../changes/app-theming/)

**Status**: in-progress

## Purpose

TBD — created by change `app-theming`. Full requirements land in this file
when the change is archived; see
`../../changes/app-theming/specs/app-theming/spec.md` for the current delta
(`runtime.appTheme` manifest block, WCAG contrast guardrail, scoped applier
reusing `nldesign-theme-selection`'s mechanism, precedence rule).

## Requirements

TBD — see the change's delta spec (link above) until archived.
