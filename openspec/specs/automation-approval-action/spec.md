# automation-approval-action Specification

**OpenSpec changes**: [automation-approval-steps](../../changes/automation-approval-steps/)

**Status**: in-progress

## Purpose

TBD — created by change `automation-approval-steps`. Full requirements land
in this file when the change is archived; see
`../../changes/automation-approval-steps/specs/automation-approval-action/spec.md`
for the current delta (My Approvals runtime widget, direct-to-OR
approve/reject, no duplicated authorization).

## Requirements

TBD — see the change's delta spec (link above) until archived.
