# component-blocks Specification

**OpenSpec changes**: [component-blocks](../../changes/component-blocks/)

**Status**: in-progress

## Purpose

TBD — created by change `component-blocks`. Full requirements land in this
file when the change is archived; see
`../../changes/component-blocks/specs/component-blocks/spec.md` for the
current delta (`ComponentBlock` schema, save/library/insert/remap/export
lifecycle).

## Requirements

TBD — see the change's delta spec (link above) until archived.
