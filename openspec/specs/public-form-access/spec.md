# public-form-access Specification

**OpenSpec changes**: [public-forms-runtime](../../changes/public-forms-runtime/)

**Status**: in-progress

## Purpose

TBD — created by change `public-forms-runtime`. Full requirements land in
this file when the change is archived; see
`../../changes/public-forms-runtime/specs/public-form-access/spec.md` for the
current delta (`ShareToken` model, public render/submission controllers,
prefill-from-URL, per-record edit links).

## Requirements

TBD — see the change's delta spec (link above) until archived.
