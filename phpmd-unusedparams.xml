<?xml version="1.0"?>
<ruleset name="Unused formal parameters"
    xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 http://pmd.sourceforge.net/ruleset_xml_schema.xsd">

    <description>
        Second PHPMD leg: UnusedFormalParameter only.

        This rule lives in its own ruleset because PHPMD honours &lt;exclude-pattern&gt;
        ONLY as a direct child of &lt;ruleset&gt;; nested inside a &lt;rule&gt; it is parsed and
        discarded (ConductionNL/.github#155). A direct child, however, is applied by
        PDepend at file-collection time and drops the file from EVERY rule in the
        ruleset. Isolating UnusedFormalParameter here means the lib/Migration
        exclusion applies to this rule and this rule ALONE - every other rule still
        analyses lib/Migration in the main leg.

        Why lib/Migration is excluded from THIS rule: OCP\Migration\IMigrationStep
        mandates changeSchema(IOutput $output, Closure $schemaClosure, array $options)
        and preSchemaChange/postSchemaChange with the same three parameters. A step
        that needs none of them still cannot drop them. The signature cannot change.

        NOTE for openbuild specifically: this app has no lib/Migration directory today,
        so the exclusion below currently matches nothing and retires zero findings. It
        is adopted for fleet uniformity and so the correct shape is already in place if
        a migration step is ever added. The previous nested exclude-pattern in phpmd.xml
        was inert either way.

        The composer script passes --exclude '*/Resources/template/*' to BOTH legs;
        dropping it from either leg would change the analysed file set.

        The pattern is */lib/Migration/* and deliberately NOT */Migration/*. The
        broader form silently swallows ordinary classes that merely have a path
        segment of that name - measured with a positive control (PHPMD 2.15, PHP 8.4,
        absolute paths): with */lib/Migration/* a probe in lib/Service/Migration/ IS
        reported; with */Migration/* it is not reported at all. That matters here
        precisely because this repo has no lib/Migration/ - the only thing the broad
        pattern could ever match is a false exclusion. Matches the fleet-canonical
        shape in nextcloud-app-template #125, doriath #157, larpingapp #263,
        planix #315.
    </description>

    <exclude-pattern>*/lib/Migration/*</exclude-pattern>

    <rule ref="rulesets/unusedcode.xml/UnusedFormalParameter"/>
</ruleset>
